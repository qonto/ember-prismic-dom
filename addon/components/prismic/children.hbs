{{~#each @node.children as |child|~}}
  <Prismic::Element
    @linkResolver={{@linkResolver}}
    @node={{child}}
    @onUnknownTag={{@onUnknownTag}}
  />
{{~else~}}
  {{~@node.element.text~}}
{{~/each~}}